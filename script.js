// Footer year
document.getElementById('year').textContent = new Date().getFullYear();

// Note: the scrolling ticker and the row hover-invert on projects are
// handled entirely in CSS (see .ticker / .project-row in style.css) —
// no JS needed for those. This file is kept small on purpose; add
// interactivity here if you introduce project detail pages later.
